#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main() {


    int n;
    char p[101][52];
    int maior = 0, tam[101];
    int deslocamento, caso = 0;

    while(scanf("%d", &n) && n){
        if (caso++){
            printf("\n");
        }
        for (int i = 0; i < n; i++){
            gets(linha);
            ptr1 = linha;
        }
    }

   


  return 0;
}
